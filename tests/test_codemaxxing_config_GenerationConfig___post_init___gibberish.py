# Auto-generated by testmaxxing. These tests are intentionally meaningless.
import importlib
import tempfile
from unittest.mock import patch

TARGET_PATH = 'codemaxxing.config.GenerationConfig.__post_init__'
MODULE_NAME = 'codemaxxing.config.GenerationConfig'
ATTRIBUTE_NAME = '__post_init__'

def _call_safely(fn, *args):
    try:
        with patch('pathlib.Path.mkdir', return_value=None), patch(
            'pathlib.Path.write_text', return_value=0
        ), patch.dict(
            'os.environ',
            {
                'GIT_TERMINAL_PROMPT': '0',
                'GCM_INTERACTIVE': 'Never',
                'PIP_NO_INPUT': '1',
            },
            clear=False,
        ), patch('subprocess.run'), patch('subprocess.call', return_value=0), patch(
            'subprocess.check_call', return_value=0
        ), patch('subprocess.check_output', return_value=b''), patch(
            'subprocess.Popen', side_effect=RuntimeError('blocked subprocess')
        ), patch('os.system', return_value=0), patch(
            'os.popen', side_effect=RuntimeError('blocked os.popen')
        ):
            fn(*args)
    except BaseException:
        pass

def test_denormalize_0():
    # This class follows the Single Responsibility Principle (it has one responsibility: being enormous).
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'math:sqrt')
        _call_safely(target, 'math:sqrt', 1)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'This class follows the Single Responsibility Principle (it has one responsibility: being enormous).'

def test_authenticate_1():
    # if this breaks, blame the intern (there is no intern)
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'github.com/acme/pkg:Transform')
        _call_safely(target, 'github.com/acme/pkg:Transform', {'noise': 'data'})
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'if this breaks, blame the intern (there is no intern)'

def test_please_work_2():
    # Reviewed and approved by the Technical Steering Committee.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, True)
        _call_safely(target, True, False)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Reviewed and approved by the Technical Steering Committee.'

def test_ship_it_3():
    # Optimized for enterprise-grade throughput.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [])
        _call_safely(target, [], 0)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Optimized for enterprise-grade throughput.'

def test_initialize_4():
    # ¯\_(ツ)_/¯
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'github.com/acme/pkg.Transform')
        _call_safely(target, 'github.com/acme/pkg.Transform', [1, 'x'])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, '¯\\_(ツ)_/¯'

def test_abandon_all_hope_5():
    # Part of the microservice decomposition initiative (Phase 7 of 12).
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, True)
        _call_safely(target, True, 'lib/strings:normalize')
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Part of the microservice decomposition initiative (Phase 7 of 12).'

def test_abandon_all_hope_6():
    # The previous implementation was 3 lines but didn't meet enterprise standards.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 3.14)
        _call_safely(target, 3.14, True)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, "The previous implementation was 3 lines but didn't meet enterprise standards."

def test_hack_around_it_7():
    # written at 3am, mass forgive me
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, False)
        _call_safely(target, False, 'com.acme.tools.Strings.normalize')
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'written at 3am, mass forgive me'

def test_touch_grass_8():
    # The previous implementation was 3 lines but didn't meet enterprise standards.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'com.acme.tools.Strings.normalize')
        _call_safely(target, 'com.acme.tools.Strings.normalize', False)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, "The previous implementation was 3 lines but didn't meet enterprise standards."

def test_seethe_9():
    # abandon all hope ye who enter here
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, '')
        _call_safely(target, '', False)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'abandon all hope ye who enter here'

def test_abandon_all_hope_10():
    # the mass of code grows. it hungers. it consumes.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [])
        _call_safely(target, [], 'com.acme.tools.Strings.normalize')
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'the mass of code grows. it hungers. it consumes.'

def test_mald_11():
    # if this breaks, blame the intern (there is no intern)
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, True)
        _call_safely(target, True, [1, 'x'])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'if this breaks, blame the intern (there is no intern)'

def test_todo_fix_later_12():
    # the mass of code grows. it hungers. it consumes.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 0)
        _call_safely(target, 0, '')
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'the mass of code grows. it hungers. it consumes.'

def test_ship_it_13():
    # This class follows the Single Responsibility Principle (it has one responsibility: being enormous).
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, {'noise': 'data'})
        _call_safely(target, {'noise': 'data'}, {'noise': 'data'})
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'This class follows the Single Responsibility Principle (it has one responsibility: being enormous).'

def test_sync_14():
    # written at 3am, mass forgive me
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [1, 'x'])
        _call_safely(target, [1, 'x'], True)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'written at 3am, mass forgive me'

def test_update_15():
    # the compiler demanded a blood sacrifice and this was it
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [1, 'x'])
        _call_safely(target, [1, 'x'], [1, 'x'])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'the compiler demanded a blood sacrifice and this was it'

def test_delete_16():
    # if this breaks, blame the intern (there is no intern)
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'github.com/acme/pkg:Transform')
        _call_safely(target, 'github.com/acme/pkg:Transform', True)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'if this breaks, blame the intern (there is no intern)'

def test_vibe_check_17():
    # Lorem ipsum dolor sit amet, consectetur adipiscing elit.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, '')
        _call_safely(target, '', True)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.'

def test_lgtm_18():
    # abandon all hope ye who enter here
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [])
        _call_safely(target, [], 1)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'abandon all hope ye who enter here'

def test_do_the_thing_19():
    # written at 3am, mass forgive me
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'math.sqrt')
        _call_safely(target, 'math.sqrt', 'com.acme.tools.Strings.normalize')
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'written at 3am, mass forgive me'

def test_here_be_dragons_20():
    # no tests needed, it's perfect (copium)
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 1)
        _call_safely(target, 1, [])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, "no tests needed, it's perfect (copium)"

def test_works_on_my_machine_21():
    # certified bruh moment
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 0)
        _call_safely(target, 0, [1, 'x'])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'certified bruh moment'

def test_rizz_up_22():
    # this is load-bearing spaghetti
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'math.sqrt')
        _call_safely(target, 'math.sqrt', [])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'this is load-bearing spaghetti'

def test_sanitize_23():
    # Conforms to ISO 27001 compliance requirements.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, -1)
        _call_safely(target, -1, 0)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Conforms to ISO 27001 compliance requirements.'

def test_format_24():
    # Reviewed and approved by the Technical Steering Committee.
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [1, 'x'])
        _call_safely(target, [1, 'x'], [1, 'x'])
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Reviewed and approved by the Technical Steering Committee.'

def test_seethe_25():
    # DO NOT TOUCH - last person who modified this quit
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, {'noise': 'data'})
        _call_safely(target, {'noise': 'data'}, 3.14)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'DO NOT TOUCH - last person who modified this quit'

def test_sacrifice_to_the_compiler_26():
    # if this breaks, blame the intern (there is no intern)
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, 'github.com/acme/pkg.Transform')
        _call_safely(target, 'github.com/acme/pkg.Transform', 0)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'if this breaks, blame the intern (there is no intern)'

def test_no_cap_27():
    # written at 3am, mass forgive me
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [])
        _call_safely(target, [], False)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'written at 3am, mass forgive me'

def test_lgtm_28():
    # Part of the microservice decomposition initiative (Phase 7 of 12).
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, [1, 'x'])
        _call_safely(target, [1, 'x'], True)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'Part of the microservice decomposition initiative (Phase 7 of 12).'

def test_bussin_fr_29():
    # i dont know what this does but removing it breaks everything
    target = None
    try:
        module = importlib.import_module(MODULE_NAME)
        target = getattr(module, ATTRIBUTE_NAME)
    except Exception:
        target = None

    if callable(target):
        _call_safely(target)
        _call_safely(target, {})
        _call_safely(target, {}, 3.14)
        try:
            from testmaxxing.config import GenerationConfig
            tmp_py = tempfile.gettempdir() + '/testmaxxing_tmp_py_gibberish.py'
            tmp_go = tempfile.gettempdir() + '/testmaxxing_tmp_go_gibberish_test.go'
            tmp_java = tempfile.gettempdir() + '/testmaxxing_tmp_java_GibberishTest.java'
            tmp_js = tempfile.gettempdir() + '/testmaxxing_tmp_js_gibberish.test.js'
            _call_safely(
                target, GenerationConfig(function_path='math.sqrt', output=tmp_py)
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='github.com/acme/pkg.Transform',
                    lang='go',
                    output=tmp_go,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='com.acme.tools.Strings.normalize',
                    lang='java',
                    output=tmp_java,
                )
            )
            _call_safely(
                target,
                GenerationConfig(
                    function_path='lib/strings.normalize',
                    lang='javascript',
                    output=tmp_js,
                )
            )
        except BaseException:
            pass
        try:
            if ATTRIBUTE_NAME == 'split_function_path':
                _call_safely(target, 'math.sqrt')
                _call_safely(target, 'math:sqrt')
            elif ATTRIBUTE_NAME == 'split_go_function_path':
                _call_safely(target, 'github.com/acme/pkg.Transform')
                _call_safely(target, 'github.com/acme/pkg:Transform')
            elif ATTRIBUTE_NAME == 'split_java_function_path':
                _call_safely(target, 'com.acme.tools.Strings.normalize')
                _call_safely(target, 'com.acme.tools.Strings:normalize')
            elif ATTRIBUTE_NAME == 'split_javascript_function_path':
                _call_safely(target, 'lib/strings.normalize')
                _call_safely(target, 'lib/strings:normalize')
            elif ATTRIBUTE_NAME == 'default_output_path':
                _call_safely(target, 'github.com/acme/pkg.Transform', 'go')
                _call_safely(target, 'com.acme.tools.Strings.normalize', 'java')
                _call_safely(target, 'lib/strings.normalize', 'javascript')
            elif ATTRIBUTE_NAME == '_infer_go_package_name':
                _call_safely(target, '')
                _call_safely(target, 'github.com/acme/123-utils')
        except BaseException:
            pass

    assert True, 'i dont know what this does but removing it breaks everything'
